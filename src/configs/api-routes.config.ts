class API_ROUTES {
	ADMIN = '/admin';
	AUTH = '/auth';
	ORDERS = `/orders`;
	PICTURES = `/pictures`;
	PRODUCTS = `/products`;
	REWIEWS = `/reviews`;
	USERS = `/users`;
}

export const API = new API_ROUTES();

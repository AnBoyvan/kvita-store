import { Metadata } from 'next';

export const metadata: Metadata = {
	title: 'Компоненти для замовлення',
	robots: { index: false, follow: false },
};

export default function CakesDashboardPage() {
	return <div>CakesDashboard</div>;
}
